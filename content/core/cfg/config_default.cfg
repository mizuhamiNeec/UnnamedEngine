bind esc togglelockcursor

bind w +forward
bind s +back
bind a +moveleft
bind d +moveright

bind e +moveup
bind q +movedown

bind ctrl +duck
bind c +duck

bind space +jump

bind mouse1 +attack1
bind mouse2 +attack2

bind r +reload

bind mwheelup +invprev
bind mwheeldown +invnext

bind f1 toggleeditor

bind 4 +bounds
bind 1 +translate
bind 2 +rotate
bind 3 +scale
bind tab +togglegizmo